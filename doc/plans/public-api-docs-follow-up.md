# Public API Documentation Follow-Up

`public_member_api_docs` is intentionally configured as an analyzer info while
protocol compliance and examples are being stabilized.

After the ACP v0.12.0 compliance work is complete, do a public API documentation
pass and decide whether to raise the lint back to a warning or error in CI.
